import { DriverPageResponse } from './driver-page-response.model';

describe('DriverPageResponse', () => {
  it('should create an instance', () => {
    expect(new DriverPageResponse()).toBeTruthy();
  });
});
