import { DeliveryResponse } from './delivery-response.model';

describe('DeliveryResponse', () => {
  it('should create an instance', () => {
    expect(new DeliveryResponse()).toBeTruthy();
  });
});
